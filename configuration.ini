[VDP]
apiKey=your_api_key
sharedSecret=your_shared_secret
cert={put the path to cert.pem under the security folder here}
key={put the path to the private key here}
visaUrl=https://sandbox.api.visa.com/
